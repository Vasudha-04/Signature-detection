Flask>=2.2
numpy
opencv-python-headless
# Pin TensorFlow to a version that typically has wheels for common deployment Python versions.
# Streamlit runners must use a compatible Python version.
tensorflow==2.15.1
Pillow
waitress>=3.0
streamlit


